[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd Server && npm install"]

[phases.build]
cmds = ["echo 'No build step needed for server'"]

[start]
cmd = "cd Server && npm start" 